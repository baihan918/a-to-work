# 高级前端 / 前端工程化 / AI Coding

## 全场景个性化面试题与参考答案

基于《宋涛-9年前端-AI方向-简历-3.2》

| **候选人定位** | 9 年前端经验；复杂业务、前端工程化、Hybrid 性能、AI Coding            |
|----------------|-----------------------------------------------------------------------|
| **核心项目**   | 有赞服务预约 0 到 1、碰碰贴 AIGC 商业化、AI Coding 流程、直播后台性能 |
| **题库范围**   | 项目深挖、基础原理、React、工程化、性能、系统设计、算法、行为与 HR    |
| **使用方式**   | 先按自己的真实经历口述，再用参考答案补结构；不要逐字背诵              |
| **关键原则**   | 团队成果讲清个人边界；量化结果准备统计口径；不知道的内容不虚构        |

> **面试前必须补齐：**构建优化和 Hybrid 性能的真实措施；直播性能优化前后证据；AI Coding 的任务样本与效率口径；碰碰贴业务数据的统计口径和个人贡献边界。

# 使用说明

- 第一遍：只看问题，尝试在 2 到 3 分钟内回答。

- 第二遍：对照参考答案，补齐“背景 - 判断 - 行动 - 结果 - 复盘”。

- 第三遍：重点训练常见追问，确保每个简历数字都能解释口径。

- 项目题回答 3 到 5 分钟；原理题 1 到 2 分钟；行为题使用 STAR，但不要机械报字母。

- 参考答案基于简历事实组织，其中需要现场补充的数据均已在“回答提醒”中标记。

## 面试叙事主线

- 复杂业务：服务预约 0 到 1、三期迭代、状态流转、复杂表单和多端链路。

- 工程化与性能：脚手架、规范包、物料库、VSCode 插件、Webpack、Hybrid H5、IM 与 SKU 性能。

- AI：AIGC 产品商业化 + AI Coding 研发流程落地，两条经历相互支撑。

- 高级工程师价值：不仅完成页面，还能做模块边界、质量、性能、协作和长期演进。

## 题库目录

| **章节** | **主题**                     | **题数** |
|----------|------------------------------|----------|
| 1        | 个人定位与简历开场           | 6        |
| 2        | AI Coding 与研发流程落地     | 8        |
| 3        | 服务预约与复杂业务系统       | 7        |
| 4        | 碰碰贴 AIGC 商业化项目       | 7        |
| 5        | 直播后台与高性能复杂交互     | 6        |
| 6        | React、状态管理与 TypeScript | 10       |
| 7        | 前端工程化、构建与组件体系   | 8        |
| 8        | 浏览器、网络、Hybrid 与性能  | 7        |
| 9        | 前端系统设计与开放题         | 5        |
| 10       | 行为面、协作与 HR            | 9        |
| 11       | 手写代码与算法高频题         | 8        |

# 一、个人定位与简历开场

### **Q1. 请做一个 3 分钟自我介绍。**

**考察点：**能否形成清晰的职业主线，而不是按年份复述简历。

**参考回答：**我有 9 年前端经验，近三年多在有赞负责零售相关业务。我的能力主线有三条：第一是复杂业务建设，例如服务预约系统从 0 到 1 并持续支持三期迭代；第二是工程化和性能治理，做过组件与 Hooks 抽象、脚手架、物料库、构建优化以及 Hybrid H5 打开速度治理；第三是 AI 方向，一方面参与碰碰贴 AIGC 营销文案产品的商业化落地，另一方面推进 AI Coding、Code Review、Jira 修复辅助等研发流程实践。我希望下一阶段继续做高级前端或前端工程化偏 AI 的岗位，把复杂业务经验和 AI 工程化能力结合起来。

**常见追问：**为什么现在想换机会；三条能力中哪一条最强；你和普通高级前端的差异是什么

**回答提醒：**不要展开所有公司经历；开场只建立“复杂业务 + 工程化性能 + AI”三条主线。

### **Q2. 为什么你的定位是“高级前端工程师、前端工程化、AI Coding 方向”？**

**考察点：**定位是否由真实经验支撑。

**参考回答：**高级前端对应我长期承担核心链路和复杂模块的经验；工程化对应我做过脚手架、规范工具、物料库、VSCode 插件、npm 组件复用和构建优化；AI Coding 则是近阶段在真实研发流程中的重点实践。我不是把三个标签简单叠加，而是希望用工程化方法解决 AI 辅助研发的上下文、质量门禁、流程治理和可评估性问题。

**常见追问：**AI Coding 做到了什么成熟度；是否真正主导；工程化里最有代表性的产出

**回答提醒：**对“主导”和“核心参与”要准确，不要把团队成果全部说成个人成果。

### **Q3. 你 9 年经历中最能代表你能力跃迁的三个阶段是什么？**

**考察点：**职业复盘和成长性。

**参考回答：**第一阶段是早期多业务后台和低代码活动系统，建立了完整交付能力；第二阶段是在爱库存和 Perfma 处理高频 IM、大量 SKU、组件复用、物料库及构建优化，从“完成页面”转向性能和研发效率；第三阶段是在有赞承担服务预约 0 到 1、Hybrid 性能和 AIGC 商业化，同时把 AI Coding 接入研发流程，从模块开发进一步走向业务结果和团队流程。

**常见追问：**哪个阶段成长最快；哪项能力仍有短板

### **Q4. 你最有竞争力的项目是哪一个？为什么？**

**考察点：**项目选择和价值判断。

**参考回答：**我会选择有赞零售服务预约与碰碰贴 AI 商业化项目。它同时覆盖 0 到 1 复杂业务、后台和用户端多端协同、AIGC 异步链路、Hybrid 性能以及真实商业结果。这个项目能体现我不只是实现功能，还能处理长期迭代、模块边界、异常体验和业务转化承接。

**常见追问：**如果只能讲一个技术难点讲什么；你的不可替代性在哪里

### **Q5. 你在项目里通常扮演什么角色？**

**考察点：**高级工程师的 ownership。

**参考回答：**我通常是核心前端或主要负责人，负责把需求转成前端方案，明确模块边界和状态模型，完成核心页面与复杂交互，并推动组件抽象、性能治理和交付质量。跨角色上会和产品、后端、测试对齐规则、接口和验收口径；但我会如实区分自己负责的前端范围与整体项目负责人范围。

**常见追问：**怎么推动别人；遇到跨团队阻塞怎么办

### **Q6. 你为什么从业务前端逐渐转向 AI 与工程化？**

**考察点：**动机是否长期、理性。

**参考回答：**我不是离开业务，而是发现复杂业务交付中的重复劳动和质量波动可以通过工程化与 AI 改善。过去做脚手架、物料库和构建优化，本质上就是把个人经验变成团队能力；AI Coding 进一步扩大了这个方向，但它仍然需要真实业务上下文、工程约束和质量验证，这正好与我的积累吻合。

**常见追问：**是否会继续写业务；为什么不转纯后端或算法

# 二、AI Coding 与研发流程落地

### **Q7. 完整介绍一下 AI Coding 在团队研发流程中的落地实践。**

**考察点：**是否真正理解端到端研发流程，而不只是会用代码助手。

**参考回答：**项目从研发链路拆解开始，覆盖 PRD 理解、技术方案、代码实现、Review、缺陷修复和性能风险识别。我的工作重点是梳理每个环节需要的上下文和约束，例如业务目标、页面结构、接口契约、边界条件、验收标准、仓库规范和相关代码，再让 AI 在限定范围内执行。输出不能直接进入生产，还需要类型检查、Lint、测试、人工 Review 和业务验收。最终沉淀的是上下文模板、Prompt、检查规则和使用边界，而不是某一个模型的使用技巧。

**常见追问：**具体使用什么模型或工具；如何衡量效果；最失败的一次实践

**回答提醒：**简历没有给出明确效率百分比，面试时不要临时编数字；可给出真实样本和评估方法。

### **Q8. 什么是你理解的 Vibe Coding？和普通 Copilot 补全有什么不同？**

**考察点：**概念边界和工程认知。

**参考回答：**普通补全主要依赖当前文件附近上下文，适合局部编码；我理解的 Vibe Coding 是让 AI 基于结构化需求、仓库约束、已有实现和验收标准完成更完整的任务。它的关键不在“一句话生成页面”，而在上下文工程、任务拆分、执行边界、结果验证和可回滚性。复杂业务里，人仍负责目标、约束和最终判断。

**常见追问：**如何防止 AI 自作主张；什么任务不适合 Vibe Coding

### **Q9. 如何为 AI 准备高质量研发上下文？**

**考察点：**上下文工程方法。

**参考回答：**我会分成事实、约束、目标和验证四层。事实包括 PRD、设计、接口、现有代码；约束包括技术栈、目录、组件复用、业务边界和禁止修改项；目标说明本次改动的用户价值及范围；验证包括验收用例、类型检查、测试和性能要求。上下文应按任务相关性裁剪，避免把整个仓库无差别塞给模型。

**常见追问：**上下文过长怎么办；如何跨会话保留项目知识；如何处理冲突信息

### **Q10. AI Code Review 应重点检查什么？**

**考察点：**代码审查体系。

**参考回答：**我会把规则分为正确性、可维护性、性能和安全边界。正确性关注空值、异常分支、状态竞争、边界条件；可维护性关注重复逻辑、组件职责、类型表达和隐式耦合；性能关注重复渲染、大列表、昂贵计算和资源加载；此外检查敏感信息、权限和不安全输入。AI 适合做第一轮广度扫描，但业务语义和架构取舍仍需人工负责。

**常见追问：**如何控制误报；如何把 Review 结果接入 CI

### **Q11. 如何实现 Jira 缺陷修复辅助？**

**考察点：**问题定位能力和工具链设计。

**参考回答：**输入不应只有 Jira 标题，而要组合复现步骤、期望与实际、日志或截图、影响版本、业务模块、相关代码和最近变更。AI 先形成问题假设和影响范围，再给定位路径与候选修复点；开发者验证后才修改。修复后要补回归用例，并检查类似代码路径，避免只修表象。

**常见追问：**如何自动找到相关代码；如何避免错误修复

### **Q12. 如何识别 AI 生成代码中的性能风险？**

**考察点：**AI 与前端性能结合。

**参考回答：**静态阶段检查渲染期间复杂计算、非稳定引用、Context 大范围更新、列表缺少虚拟化、事件和定时器未清理、资源同步加载等模式；运行阶段结合 React Profiler、Performance、长任务、Web Vitals 和业务埋点验证。规则只能提示风险，是否优化要结合数据、用户路径和维护成本。

**常见追问：**哪些规则容易误报；如何做自动修复

### **Q13. 如何评价 AI Coding 是否真的提效？**

**考察点：**指标意识。

**参考回答：**不能只看生成代码量。我会观察需求理解到首个可运行版本的时间、Review 往返次数、缺陷修复定位时间、线上缺陷率、返工率以及开发者接受度。同时要区分简单任务和复杂任务，建立对照样本。若速度提高但 Review 成本和缺陷上升，就不算真正提效。

**常见追问：**如何设计实验；你目前掌握了哪些真实数据

**回答提醒：**准备 2 到 3 个真实任务样本：任务规模、传统耗时、AI 参与方式、节省环节、额外校验成本。

### **Q14. AI Coding 落地最大的风险是什么？**

**考察点：**成熟度和风险意识。

**参考回答：**主要风险是上下文缺失导致看似正确但业务错误、开发者过度信任、隐私与代码泄漏、输出不可复现，以及为了追求生成速度引入长期技术债。治理上要做权限和数据分级、明确禁止输入、限制修改范围、保留变更记录，并把类型检查、测试、Review 和业务验收设为硬门禁。

**常见追问：**企业代码能否发送给外部模型；如何审计 AI 生成内容

# 三、服务预约与复杂业务系统

### **Q15. 如何从 0 到 1 设计服务预约系统的前端架构？**

**考察点：**复杂业务建模与模块拆分。

**参考回答：**我会先按领域拆分商家配置、预约规则、预约单、履约状态和用户预约链路，再区分领域状态、页面状态和远程数据。前端模块围绕稳定业务概念组织，而不是按页面堆文件。状态流转由后端给出权威状态与可执行动作，前端负责展示、交互约束和幂等提交；跨期扩展通过规则配置、能力开关和复用组件承接。

**常见追问：**目录怎么设计；哪些逻辑放后端；如何支持三期迭代

### **Q16. 预约状态流转怎么设计才能避免混乱？**

**考察点：**状态机意识。

**参考回答：**先明确状态集合、允许事件、前置条件和异常状态，用状态机或显式映射表达“当前状态下可展示哪些操作”。前端不自行推导最终业务状态，提交动作带幂等标识，成功后以服务端结果刷新。对取消、超时、并发操作等边界要有统一错误码和降级提示。

**常见追问：**前后端状态不一致怎么办；是否使用状态机库

### **Q17. 复杂预约表单如何设计？**

**考察点：**表单建模和可维护性。

**参考回答：**将字段模型、校验规则、联动规则和提交转换分离。静态展示用配置描述，复杂联动保留显式业务代码，避免做成难以调试的万能表单。对于时间、门店、服务人员等联动，使用派生状态而不是复制状态；异步校验处理竞态和取消；提交前统一归一化数据。

**常见追问：**React Hook Form 与自研方案怎么选；动态字段怎么做类型约束

### **Q18. 时间选择场景有哪些容易遗漏的边界？**

**考察点：**业务细节和边界意识。

**参考回答：**包括门店时区、跨天营业、节假日、服务时长、缓冲时间、提前预约和最晚取消限制、库存并发、服务人员排班以及客户端时间不可信。前端负责可视化和即时反馈，最终可预约性必须由后端校验，提交失败时应保留用户输入并给出可恢复路径。

**常见追问：**夏令时如何处理；并发抢同一时段怎么办

### **Q19. 如何支持同一套预约能力在不同零售场景扩展？**

**考察点：**抽象能力。

**参考回答：**先识别真正稳定的核心：预约对象、时间资源、预约人、状态和履约动作；场景差异通过规则、可选字段、能力开关和插槽式扩展承接。抽象要来自至少两到三个真实场景，避免在第一期预判所有变化。公共层只处理稳定机制，业务含义留在领域模块。

**常见追问：**什么时候不应该抽象；如何治理配置爆炸

### **Q20. 服务预约三期迭代中如何控制技术债？**

**考察点：**长期维护能力。

**参考回答：**每期先做影响面分析，区分短期兼容与长期模型变化；高频变化点通过规则层和适配层隔离。对临时分支设置清理条件和负责人，在需求结束后补测试与重构。通过埋点、缺陷和开发耗时识别真正需要治理的模块，不做脱离业务节奏的大重写。

**常见追问：**有没有不得不妥协的案例；如何说服产品留重构时间

### **Q21. 你如何与产品、后端、测试共同完成 0 到 1 项目？**

**考察点：**跨团队协作。

**参考回答：**需求阶段把关键名词、状态、边界和验收标准对齐；方案阶段明确前后端职责、接口契约、异常码和灰度策略；开发阶段用可运行版本提前验证复杂交互；联调阶段按核心链路和异常链路分层；上线前确认监控、回滚和数据口径。争议尽量落到用户路径、数据和维护成本上。

**常见追问：**需求频繁变化怎么办；后端接口延迟怎么办

# 四、碰碰贴 AIGC 商业化项目

### **Q22. 请介绍碰碰贴 AI 营销文案生成链路。**

**考察点：**AI 产品的前端工程能力。

**参考回答：**后台侧配置营销场景、文案类型和生成参数；小程序侧收集用户选择和输入，调用后端 AIGC 服务，展示生成中、成功、失败和可重试状态，支持结果编辑并承接后续线索转化。前端重点不在模型推理，而在参数完整性、异步状态、结果可编辑性、失败恢复和业务转化体验。

**常见追问：**为什么不直接在前端调用模型；生成超时如何处理

### **Q23. AIGC 请求的状态管理如何设计？**

**考察点：**异步状态与竞态处理。

**参考回答：**建议显式建模 idle、submitting、generating、success、failed、cancelled 等状态，并保存 requestId 或 taskId。重复点击要禁用或幂等；轮询需退避、超时和页面卸载清理；用户重新生成时要区分旧结果和新任务，避免晚到响应覆盖当前状态。长任务最好由服务端任务化，前端负责查询或订阅。

**常见追问：**SSE、WebSocket、轮询怎么选；刷新页面后如何恢复

### **Q24. 如何设计 AI 生成失败的异常兜底？**

**考察点：**产品体验与鲁棒性。

**参考回答：**先按可重试、需修改输入、系统不可用和内容安全限制分类。提示应说明下一步，而不是只显示“生成失败”；保留用户输入，允许原参数重试或局部修改；连续失败时提供模板文案或人工编辑入口。异常上报包含任务阶段、错误类型和耗时，但不泄露敏感内容。

**常见追问：**限流怎么提示；内容审核拒绝怎么处理

### **Q25. 如何防止重复生成和重复计费？**

**考察点：**幂等与商业系统意识。

**参考回答：**前端用请求锁和明确加载态减少重复点击，但最终依赖后端幂等键、任务状态和计费事务。网络重试应复用同一业务请求标识，不能每次创建新任务。结果页可基于 taskId 恢复，前端展示的次数和权益以服务端为准。

**常见追问：**请求成功但响应丢失怎么办；多端同时操作怎么办

### **Q26. 你如何理解项目中 500W+ 业务收入与前端工作的关系？**

**考察点：**业务价值表达是否克制准确。

**参考回答：**这属于整个产品和销售链路共同产生的业务结果，不能归因于前端个人。我的贡献是完成生成能力在后台和小程序端的可用落地，保障参数配置、生成反馈、编辑重试、异常恢复和线索转化链路顺畅，从而支撑产品对外售卖和转化。面试中我会同时说明数据口径和个人负责边界。

**常见追问：**具体优化过哪个转化节点；数据来自哪里

**回答提醒：**准备清楚 29.8W+、10.3W+、2W+、7W+、564、500W+ 各自口径、统计时间和团队归因。

### **Q27. 后台 AI 生成策略配置如何避免失控？**

**考察点：**配置系统设计。

**参考回答：**配置要有 schema、类型校验、默认值、版本、预览、发布和回滚机制；高风险参数限制范围并做权限控制。前端表单根据 schema 渲染但保留明确业务组件，发布前展示差异并进行服务端校验。配置变更需要审计记录，客户端按版本或灰度策略读取。

**常见追问：**动态表单如何做；旧客户端不认识新配置怎么办

### **Q28. 如何评估 AI 文案产品的质量？**

**考察点：**AI 产品指标。

**参考回答：**技术层看成功率、首 token 或首结果耗时、整体耗时、重试率和错误分布；产品层看生成完成率、编辑率、复制或使用率、二次生成率、线索转化和用户反馈。高编辑率不一定差，关键看编辑幅度和最终使用。前端埋点要围绕完整漏斗，而不是只统计按钮点击。

**常见追问：**如何做 A/B 实验；流式输出是否一定更好

# 五、直播后台与高性能复杂交互

### **Q29. 高频 IM 消息为什么会导致 React 页面卡顿？**

**考察点：**渲染性能原理。

**参考回答：**消息频率高时，每条消息触发状态更新会造成频繁调度和组件树渲染；数组不断复制、排序或派生计算会增加主线程工作；长列表 DOM 增长还会带来布局和绘制成本。优化要从消息聚合、状态粒度、数据结构、虚拟列表和渲染优先级共同入手。

**常见追问：**如何定位瓶颈；React 18 能解决多少

### **Q30. 你会如何优化高频 IM 消息渲染？**

**考察点：**系统化优化方案。

**参考回答：**传输层按帧或时间窗口批量提交；数据层用 Map 加有序 id，避免反复全量遍历；渲染层使用虚拟列表、稳定 key 和细粒度订阅；低优先级统计可延迟更新。消息上限和历史分页也要控制 DOM 与内存。最后通过 Performance、Profiler 和业务帧率验证。

**常见追问：**批量窗口设多大；如何保证消息顺序

### **Q31. 大量 SKU 组合为什么容易出现性能问题？**

**考察点：**组合数据建模。

**参考回答：**规格维度的笛卡尔积会导致数据量快速膨胀，若每次编辑都重建全部组合、深拷贝整棵对象或渲染所有行，就会卡顿。应使用稳定 SKU key、增量生成、结构共享、虚拟化表格和字段级更新，并避免把可派生数据重复存入状态。

**常见追问：**如何生成笛卡尔积；规格变化时如何保留已有输入

### **Q32. ImmerJS 在复杂表单里有什么优缺点？**

**考察点：**状态管理取舍。

**参考回答：**优点是用可变写法表达不可变更新，适合深层业务对象，并通过结构共享保留未修改引用。缺点是大对象高频更新仍有代理和 finalize 成本，也可能让开发者忽略状态粒度。使用时要缩小 produce 范围，避免在渲染热点中频繁处理超大对象，并结合性能数据选择。

**常见追问：**Immer 是否能避免 Context 消费者重渲染；什么时候不用 Immer

### **Q33. 如何设计直播推拉流状态展示？**

**考察点：**实时状态与容错。

**参考回答：**区分业务状态、媒体连接状态和 UI 状态，例如未开播、连接中、推流中、重连、结束及异常。状态来源要有优先级，服务端权威状态与客户端即时状态结合；断网或心跳丢失进入可恢复状态而非直接判定结束。操作要防重复，并保留诊断信息。

**常见追问：**WebSocket 断线重连怎么做；状态乱序怎么办

### **Q34. 如何证明性能优化有效？**

**考察点：**性能测量。

**参考回答：**先建立可复现基线：固定消息频率、SKU 数量、设备和操作路径。记录主线程长任务、commit 次数和耗时、交互延迟、内存和帧率；优化后用同一场景对比，并观察线上分位数。不能只凭“感觉更流畅”。

**常见追问：**用哪些工具；线上如何监控

**回答提醒：**简历未写具体优化百分比，准备真实压测规模和前后指标；没有数据就明确说当时主要以哪些证据验收。

# 六、React、状态管理与 TypeScript

### **Q35. React 一次更新经历哪些阶段？**

**考察点：**React 渲染机制。

**参考回答：**更新先被调度，render 阶段计算新的 Fiber 树并进行 reconciliation，这一阶段可中断；commit 阶段同步提交 DOM 变更，执行 layout effect 的清理与创建；浏览器绘制后再处理 passive effect。组件函数执行不等于 DOM 一定变化。

**常见追问：**useLayoutEffect 和 useEffect 区别；render 阶段为什么要求纯函数

### **Q36. Context 的 value 引用变化会发生什么？**

**考察点：**React 状态传播。

**参考回答：**Provider 的 value 使用 Object.is 比较，引用变化会通知所有订阅该 Context 的消费者重新渲染，即使组件只读取对象中的另一个字段。React.memo 不能阻止 Context 更新导致的消费者执行。可以拆分 Context、稳定 value、采用 selector 或外部 store 的细粒度订阅。

**常见追问：**配合 Immer 是否解决；Redux 为什么可以更细粒度

### **Q37. React.memo、useMemo、useCallback 应该怎么使用？**

**考察点：**优化判断。

**参考回答：**它们是缓存工具，不是默认模板。React.memo 适合 props 稳定且渲染成本明显的组件；useMemo 缓存昂贵计算或需要稳定引用的值；useCallback 主要在回调作为 memo 子组件 props 或依赖项时有价值。先用 Profiler 找热点，缓存也有比较和内存成本。

**常见追问：**为什么滥用反而变慢；React Compiler 会改变什么

### **Q38. 如何避免 useEffect 中的竞态？**

**考察点：**副作用与异步。

**参考回答：**每次 effect 建立对应的取消机制，例如 AbortController 或序号标记；清理阶段使旧请求失效，回调只提交当前请求结果。依赖应完整，避免闭包读取过期值。更复杂的远程数据优先交给具备缓存、去重和取消能力的数据层。

**常见追问：**setState 算不算副作用；Strict Mode 为什么执行两次

### **Q39. 何时选择 Context、Redux/Zustand 或服务端状态库？**

**考察点：**状态分类。

**参考回答：**Context 适合低频、范围明确的依赖注入，如主题和权限；Redux/Zustand 适合跨模块客户端状态、复杂事件流和细粒度订阅；服务端状态库处理请求缓存、失效、去重和重试。选择前先区分 UI 局部状态、领域客户端状态和服务端状态，不把所有数据塞进一个全局 store。

**常见追问：**预约系统状态如何拆；表单状态是否放 Redux

### **Q40. 如何设计一个可复用的业务 Hook？**

**考察点：**Hooks 抽象。

**参考回答：**Hook 应封装稳定的状态机制和副作用，而不是隐藏所有业务。输入输出要清晰，返回状态、动作和必要元数据；不要依赖隐式全局变量；处理清理、并发和错误；复杂业务允许通过策略参数或回调扩展。抽象前先验证多个调用方确实共享同一机制。

**常见追问：**Hook 返回对象还是数组；如何测试 Hook

### **Q41. TypeScript 中如何表达后端接口的状态联合？**

**考察点：**类型建模。

**参考回答：**使用可辨识联合，以 status 或 type 作为判别字段，让不同状态携带各自合法字段；通过 switch 穷尽检查，避免大量可选属性。例如生成中有 taskId 和 progress，成功有 result，失败有 errorCode。运行时仍需 schema 校验，因为 TypeScript 不能保证网络数据真实。

**常见追问：**never 穷尽检查怎么写；zod 一类工具的价值

### **Q42. interface 与 type 怎么选？**

**考察点：**TypeScript 基础与实践。

**参考回答：**对象公共契约和需要声明合并或 extends 时常用 interface；联合、交叉、条件类型和映射类型用 type 更自然。团队一致性比机械规则重要。对于业务模型，我更关注是否准确表达不变量，而不是纠结关键字。

**常见追问：**泛型约束怎么用；any、unknown、never 的区别

### **Q43. 如何处理超大列表？**

**考察点：**列表性能。

**参考回答：**先减少数据与渲染范围：服务端分页或游标、虚拟列表、增量加载。然后保证 key 稳定、行组件细粒度订阅、避免每行创建昂贵对象和函数。动态高度需要测量缓存与滚动锚定；筛选排序放到合适线程或服务端。无障碍和键盘导航也不能因虚拟化丢失。

**常见追问：**动态高度虚拟列表难点；列表更新如何保持滚动位置

### **Q44. 受控组件和非受控组件如何取舍？**

**考察点：**表单原理。

**参考回答：**受控组件状态统一、联动和即时校验容易，但大表单每次输入可能引发更多更新；非受控组件更接近 DOM，性能好但跨字段同步较弱。复杂表单可以用字段级订阅或 React Hook Form 一类方案，在可控性和更新成本之间平衡。

**常见追问：**Ant Design Form 属于什么模式；如何优化百字段表单

# 七、前端工程化、构建与组件体系

### **Q45. 你在 Perfma 参与 Webpack 构建优化时，如何定位和解决构建瓶颈？**

**考察点：**构建优化的方法、测量意识以及个人贡献边界。

**参考回答：**我会先区分冷启动、增量构建和生产构建，固定机器、分支与缓存条件建立基线，再通过 profiling 分析 loader、插件、模块解析和产物生成的耗时。优化方向包括缩小 loader 处理范围、启用持久化缓存、减少重复编译、调整 source map、优化依赖解析与拆包策略。最后使用同一场景多次测量，并说明自己实际负责和参与的部分。

**常见追问：**当时最大的瓶颈是什么；哪些措施没有效果；为什么没有直接迁移到 Vite

**回答提醒：**3.2 简历没有构建提速百分比，不要主动补充无法证明的量化结果；重点讲定位过程、具体措施和验证方法。

### **Q46. Webpack 与 Vite 的核心差异是什么？**

**考察点：**构建工具原理。

**参考回答：**Webpack 开发期通常以 bundle 为中心，生态成熟、定制能力强；Vite 开发期利用浏览器原生 ESM 和依赖预构建按需提供模块，启动和更新通常更快，生产构建多基于 Rollup。迁移选择取决于旧项目插件、模块兼容、微前端、部署和定制成本。

**常见追问：**Vite 为什么要预构建依赖；HMR 原理

### **Q47. 如何设计团队统一的 ESLint、StyleLint、CommitLint 工具包？**

**考察点：**规范工程化。

**参考回答：**将配置以版本化 npm 包发布，提供不同技术栈 preset 和少量可覆盖项；配套 CLI 完成初始化、依赖检查和配置生成；在本地 pre-commit 做快速检查，在 CI 做完整检查。升级要有 changelog、迁移说明和灰度，规则强调可自动修复和真实价值，避免为规范而规范。

**常见追问：**规则冲突怎么处理；如何推动老项目接入

### **Q48. 物料库、组件库和业务组件库有什么区别？**

**考察点：**复用体系。

**参考回答：**基础组件库解决通用 UI 和交互；业务组件库封装稳定领域能力；物料库覆盖模板、区块、页面片段和生成工具，目标是提高交付速度。三者需要明确依赖方向、版本策略、文档和示例，避免业务组件反向污染基础组件。

**常见追问：**如何判断组件是否值得沉淀；如何解决版本碎片

### **Q49. 通过 npm 包复用多平台模块有哪些难点？**

**考察点：**包设计和发布治理。

**参考回答：**难点包括公共契约是否稳定、peerDependencies、样式隔离、构建格式、类型声明、宿主环境差异和版本兼容。包应尽量无宿主副作用，通过适配器注入平台能力；采用语义化版本、自动测试和变更日志，并给出升级路径。

**常见追问：**monorepo 是否更合适；如何本地联调

### **Q50. VSCode 插件如何支持物料生成和环境切换？**

**考察点：**开发工具能力。

**参考回答：**插件层负责命令、交互和编辑器集成，核心生成逻辑应下沉为可测试的 Node.js 包或 CLI。物料生成基于模板、schema 和变量校验；环境切换修改受控配置或调用内部服务，并保留当前状态和回滚。注意工作区信任、路径安全和不要写入敏感信息。

**常见追问：**插件生命周期；如何发布和升级

### **Q51. 如何做前端产物体积优化？**

**考察点：**构建产物分析。

**参考回答：**先用 bundle analyzer 确认大依赖、重复依赖和不可摇树模块；再做路由或功能拆包、按需加载、依赖替换、tree shaking、sideEffects 标注、资源压缩和现代浏览器差异化输出。目标不是体积最小，而是关键路径加载更快并控制缓存失效范围。

**常见追问：**拆包过细有什么问题；如何设计长期缓存

### **Q52. 如何治理一个历史包袱重的前端项目？**

**考察点：**渐进式改造。

**参考回答：**先建立可观测基线和边界：构建时间、缺陷、核心路径、模块依赖。优先处理阻塞交付和高风险区域，通过适配层、模块绞杀、测试补齐和新旧规范并存逐步迁移。每次业务需求顺带改善局部，不做一次性大重写；用指标证明改造收益。

**常见追问：**什么时候必须重写；如何避免双轨成本失控

# 八、浏览器、网络、Hybrid 与性能

### **Q53. 从输入 URL 到页面可交互发生了什么？**

**考察点：**浏览器和网络全链路。

**参考回答：**包括 URL 解析、缓存与 Service Worker 检查、DNS、连接与 TLS、HTTP 请求响应、HTML 流式解析、CSSOM 和 DOM 构建、脚本执行、样式计算、布局、绘制和合成。框架还需下载执行 JS、获取数据和 hydration 或客户端渲染。优化应针对真实关键路径，而不是背步骤。

**常见追问：**preload 与 prefetch；HTTP/2 与 HTTP/3 差异

### **Q54. Hybrid H5 打开速度慢，你如何系统定位？**

**考察点：**Hybrid 性能治理。

**参考回答：**把链路拆成容器启动、WebView 创建、离线包或网络资源、HTML/JS 执行、首屏接口、React 渲染和 JSBridge 通信，分别打点。结合冷启动与热启动、网络环境、机型和版本看分位数，找到主导瓶颈后再优化。不能只看网页 Performance，因为容器前置耗时可能占比很高。

**常见追问：**如何设计打点；白屏时间怎么算
> Hybrid 场景更推荐拆成两段：
> Native 点击入口 -> WebView 开始 loadUrl
> WebView 开始 loadUrl -> H5 FCP

### **Q55. Hybrid 页面有哪些常见优化手段？**

**考察点：**端到端性能。

**参考回答：**容器侧可预创建 WebView、预加载和复用；资源侧用离线包、版本校验、增量更新和长期缓存；页面侧减少首屏 JS、延迟非关键模块、并行请求、骨架屏和局部渲染；桥接侧合并调用、缓存稳定信息并减少往返。要同时关注包更新一致性和回滚。

**常见追问：**离线包如何防止版本错配；预加载的内存代价

### **Q56. JSBridge 如何设计？**

**考察点：**跨端通信协议。

**参考回答：**JSBridge 我会设计成 H5 和 Native 之间统一的协议层，而不是让业务直接调用系统通道。H5 侧提供 JSBridge SDK，统一封装 call、on、ready、canIUse；协议里用 requestId 匹配异步回调，用 module/method 做能力分组，用 code/message/data 统一返回。Native 侧按模块注册能力，并做白名单、权限、参数校验。版本兼容上通过 bridgeVersion 和 capabilities 做能力协商，避免新 H5 调旧 Native 不支持的方法。错误处理上要有超时、取消、不支持、权限拒绝等统一错误码。整体目标是让 Bridge 调用可控、可观测、可降级、可演进。

**常见追问：**回调如何防泄漏；如何防止任意原生能力调用

### **Q57. 浏览器缓存如何设计？**

**考察点：**HTTP 缓存。

**参考回答：**带内容 hash 的静态资源使用长时间强缓存和 immutable；HTML 或入口清单短缓存或协商缓存，保证能获取新版本；接口根据业务一致性使用 Cache-Control、ETag 或应用缓存。发布时先上传资源再切入口，避免 HTML 引用不存在的资源。

**常见追问：**304 是否有响应体；Service Worker 与 HTTP 缓存关系

### **Q58. 什么是长任务，如何优化？**

**考察点：**主线程性能。

**参考回答：**主线程连续执行超过约 50ms 的任务会阻塞输入和渲染。通过 Performance 和 Long Tasks 定位脚本、渲染或第三方代码，将大任务切片、降低算法复杂度、延迟非关键工作、使用 Worker，并减少不必要渲染。还要关注 INP，因为用户交互路径比单个任务更有业务意义。

**常见追问：**requestIdleCallback 能否解决；Worker 适合什么计算

### **Q59. 骨架屏提升的是性能还是体验？**

**考察点：**性能指标与用户感知。

**参考回答：**骨架屏主要改善感知和过程反馈，不会自动降低真实加载时间，甚至可能增加资源和渲染成本。它应与减少关键资源、并行接口和局部加载配合；形态要接近最终布局以避免 CLS，并避免加载很快时闪烁。

**常见追问：**如何设置最短展示时间；什么时候不用骨架屏

# 九、前端系统设计与开放题

### **Q60. 设计一个企业级 AI 对话前端。**

**考察点：**AI 应用系统设计。

**参考回答：**模块包括会话与消息模型、流式传输、输入与附件、工具调用状态、引用展示、错误重试和会话持久化。流式响应使用 SSE 或 fetch stream，增量解析与批量渲染避免每 token 更新；消息按服务端 id 幂等合并。长会话采用服务端分页、摘要和虚拟列表，敏感内容与权限在服务端控制。

**常见追问：**如何停止生成；断线后怎么续传；Markdown 如何安全渲染

### **Q61. 设计一个可配置营销活动搭建系统。**

**考察点：**低代码经验与架构。

**参考回答：**核心是 schema、组件协议、编辑器状态、预览渲染和发布版本。组件注册表描述属性、事件和约束；编辑器处理拖拽、选中、历史记录和校验；运行时只消费经过校验的 schema。发布需要版本、灰度和回滚，扩展能力受白名单与安全沙箱限制。

**常见追问：**撤销重做怎么做；如何支持组件升级

### **Q62. 设计一个前端监控平台。**

**考察点：**稳定性平台经验。

**参考回答：**SDK 采集 JS 错误、资源错误、请求、性能和用户行为，进行采样、脱敏、聚合并批量上报；平台按应用、版本、环境和用户维度检索，结合 source map 还原堆栈。告警要去重、聚类并设置阈值，发布系统关联版本，形成发现、定位、修复和验证闭环。

**常见追问：**跨域脚本错误怎么拿堆栈；如何控制上报成本

### **Q63. 如何设计一个可扩展的权限系统前端？**

**考察点：**后台系统通用能力。

**参考回答：**服务端负责最终授权，前端权限用于路由、菜单和操作展示。权限模型可由资源、动作和作用域组成，登录后获取能力集合并通过统一 Hook 或组件判断。路由异步加载要防止未授权代码和数据暴露；权限变更需要刷新机制和审计。

**常见追问：**RBAC 与 ABAC；按钮隐藏是否等于安全

### **Q64. 如何设计多主题或白标系统？**

**考察点：**主题定制经验。

**参考回答：**将设计 token 分为基础、语义和组件层，运行时优先用 CSS Variables，构建时可生成品牌包。组件不直接写品牌色，资源和字体通过主题清单注入。主题配置需校验对比度和缺失 token，并有默认回退；避免为每个客户 fork 代码。

**常见追问：**Ant Design 主题如何接入；旧浏览器怎么办

# 十、行为面、协作与 HR

### **Q65. 讲一次你遇到的最困难技术问题。**

**考察点：**STAR 表达和技术深度。

**参考回答：**可选高频 IM 或大量 SKU 性能问题。回答按场景、目标、定位、行动、结果展开：先说明用户可感知的问题和数据规模，再说如何通过 Profiler/Performance 区分状态更新、计算和 DOM 瓶颈，之后讲批量更新、数据结构、组件粒度或虚拟化，最后给出验证方式和沉淀。不要只列优化名词。

**常见追问：**哪些尝试失败了；如果重做会改变什么

### **Q66. 讲一次需求频繁变化的经历。**

**考察点：**应变和边界管理。

**参考回答：**可用服务预约三期迭代。先把变化区分为业务规则、页面呈现和核心模型变化；短期通过配置和适配层承接，涉及模型的变化则重新对齐状态与接口契约。同步影响范围、交付方案和技术债，避免开发团队无声吞掉变化成本。

**常见追问：**你是否拒绝过需求；如何与产品冲突

### **Q67. 讲一次你推动团队改进的经历。**

**考察点：**影响力。

**参考回答：**可讲 AI Coding 流程或统一规范工具。先从团队重复劳动或质量波动出发，用小范围真实任务验证，再沉淀模板、规则和工具，收集开发者反馈迭代，最后接入日常流程。重点说明自己如何促成采用，而不仅是完成技术实现。

**常见追问：**有人不愿使用怎么办；如何衡量推广成功

### **Q68. 你如何做技术方案评审？**

**考察点：**高级工程师方法。

**参考回答：**先确认目标、范围和不做什么；再看领域模型、数据流、接口、异常和兼容；评估性能、安全、可观测、灰度与回滚；最后比较方案成本和长期维护。评审结论应沉淀决策与待办，不把讨论停留在个人偏好。

**常见追问：**如何处理评审分歧；什么方案必须写 RFC

### **Q69. 你如何带新人或帮助同事成长？**

**考察点：**团队贡献。

**参考回答：**通过任务拆分、上下文补齐、代码 Review 和阶段复盘帮助对方建立方法，而不是直接替他完成。对新模块提供最小可运行样例和边界说明；Review 解释原因和替代方案；随着熟练度逐步放权。工程化文档和模板也能降低依赖个人口头传递。

**常见追问：**同事连续犯同样错误怎么办

### **Q70. 你的短板是什么？**

**考察点：**自我认知。

**参考回答：**可以坦诚说明相比纯后端或算法岗位，自己在模型训练和大规模后端系统上不是优势；当前优势是前端复杂业务、工程化和 AI 应用落地。改善方式是持续补充 Node/BFF、流式协议、模型能力边界和评估体系，同时保持岗位定位聚焦。

**常见追问：**为什么公司要接受这个短板

**回答提醒：**不要用“过于追求完美”这类伪短板，也不要暴露岗位核心能力缺失。

### **Q71. 为什么离开有赞？**

**考察点：**职业动机和稳定性。

**参考回答：**建议表达为：在有赞完成了零售复杂业务、AIGC 商业化和 AI Coding 流程实践的积累，下一阶段希望进入更重视 AI 应用与前端工程化结合的岗位，承担更完整的技术 ownership。肯定现团队和经历，不抱怨组织或业务。

**常见追问：**是否已离职；有赞不能提供这些机会吗

### **Q72. 你的期望岗位和未来三年规划是什么？**

**考察点：**岗位匹配。

**参考回答：**目标是高级前端或前端工程化偏 AI 的岗位，继续承担复杂业务核心模块，同时推进 AI 应用前端、研发提效和质量体系。未来三年希望形成从业务建模、前端架构、AI 交互到工程治理的完整能力，并能够影响团队交付方式。

**常见追问：**想走专家还是管理；是否接受纯业务岗位

### **Q73. 你会向面试官反问什么？**

**考察点：**判断机会的能力。

**参考回答：**可以问：团队当前最重要的业务与技术挑战是什么；这个岗位入职三个月和一年分别如何评价成功；AI 方向目前处于探索、产品化还是规模化阶段；前端在方案和工程治理中的 ownership；团队如何做质量、性能和研发效率度量。避免第一轮只问福利。

# 十一、手写代码与算法高频题

### **Q74. 实现一个支持并发数限制的异步任务调度器。**

**考察点：**Promise、队列和工程边界。

**参考回答：**维护等待队列和 running 数。add 返回 Promise，将任务及 resolve/reject 入队；run 在 running 小于 limit 时取任务执行，finally 中递减并继续调度。要考虑同步抛错、任务返回非 Promise、队列清空和是否保持结果顺序。

**常见追问：**如何支持取消；如何动态调整并发数

### **Q75. 实现 debounce 和 throttle，并说明使用场景。**

**考察点：**闭包、定时器与边界。

**参考回答：**debounce 在停止触发一段时间后执行，适合搜索输入；throttle 保证时间窗口内最多执行一次，适合滚动和拖拽。实现需保留 this 和参数，支持 leading、trailing、cancel，注意最后一次调用是否丢失。

**常见追问：**用时间戳和定时器的差异

### **Q76. 实现深拷贝需要考虑什么？**

**考察点：**JavaScript 类型系统。

**参考回答：**基础版本使用 WeakMap 处理循环引用，按类型处理 Date、RegExp、Map、Set、Array 和普通对象，并复制 symbol key 与属性描述符。函数、DOM、Promise、WeakMap 等通常不应盲目克隆。业务中优先使用 structuredClone 或针对数据模型设计复制范围。

**常见追问：**JSON 方法有哪些问题；原型链怎么处理

### **Q77. 实现一个 LRU Cache。**

**考察点：**数据结构。

**参考回答：**使用 Map 利用插入顺序：get 命中后删除再插入到末尾；set 已存在先删除，插入后超过容量则删除第一个 key。get/set 平均 O(1)。若需要更复杂节点操作可用哈希表加双向链表。

**常见追问：**如何加 TTL；并发场景怎么处理

### **Q78. 实现大数组分片处理，避免阻塞主线程。**

**考察点：**事件循环与调度。

**参考回答：**按时间预算而非固定数量切片：每轮处理直到接近 8 到 12ms，再通过 MessageChannel、scheduler 或 setTimeout 让出主线程。支持进度、取消和错误处理。CPU 密集且可序列化的任务可移到 Web Worker。

**常见追问：**为什么不用 Promise.then；requestIdleCallback 的问题

### **Q79. 设计一个 useRequest Hook。**

**考察点：**React Hook 综合能力。

**参考回答：**输入请求函数和依赖，返回 data、error、loading、run、cancel；内部使用 AbortController 或请求序号防竞态，卸载时取消；可选支持缓存、重试、轮询和防抖。请求函数引用稳定性和自动执行语义要写清，复杂场景应优先复用成熟服务端状态库。

**常见追问：**如何处理 Strict Mode；如何缓存并去重

### **Q80. 给定规格数组，生成 SKU 笛卡尔积。**

**考察点：**递归/迭代和真实项目映射。

**参考回答：**从一个空组合开始，依次遍历每个规格维度，将当前组合与该维度每个值拼接形成新组合。复杂度等于最终组合数，无法从算法上消除输出规模，因此真实业务更重要的是增量更新、稳定 key 和虚拟渲染。

**常见追问：**规格删除后如何保留输入；如何生成稳定 SKU key

### **Q81. 如何判断链表是否有环，并找到入口？**

**考察点：**基础算法。

**参考回答：**快慢指针相遇说明有环；相遇后一个指针回到头部，两者每次走一步，再次相遇处为环入口。时间 O(n)，空间 O(1)。面试时要解释数学关系，而不是只背代码。

**常见追问：**如何求环长度

# 十二、简历数字与证据清单

**原则：**面试官追问数字时，应说明统计对象、时间范围、数据来源、基线、个人动作和团队归因。

| **简历表述**                  | **必须准备**                                | **推荐表达**                                          | **风险**                         |
|-------------------------------|---------------------------------------------|-------------------------------------------------------|----------------------------------|
| Webpack 构建优化              | 冷/热构建场景、瓶颈定位、具体措施、验证方法 | “先建立基线，再通过 profiling 找到主要耗时并逐项验证” | 自行补充简历中不存在的提速百分比 |
| 29.8W+ 发出、10.3W+ 激活      | 统计截止时间、发出/激活定义、数据系统       | “这是产品整体数据，我负责前端生成与转化链路”          | 把业务总结果归功于个人           |
| 覆盖 2W+ 商家、7W+ 非有赞用户 | 去重口径、时间窗口、用户定义                | 说明覆盖规模用于证明产品已真实商业化                  | 数字之间关系解释不清             |
| 564 个主产品、500W+ 收入      | 截至 2026-02、线索归因、收入口径            | 强调支撑关系和个人负责边界                            | 说成前端直接创造全部收入         |
| AI Coding 提效                | 2-3 个任务样本、节省环节、校验成本          | 用案例和前后流程说明，不硬编百分比                    | 只说“效率明显提升”               |
| IM/SKU 性能优化               | 消息频率、SKU 规模、工具、前后指标          | 没有历史精确数字时说明验证证据                        | 凭主观感受宣称大幅提升           |

## 最终 30 秒收口

**参考表达：**我的优势不是单点技术标签，而是能把复杂业务、前端工程化和 AI 落地连起来。我做过从 0 到 1 的业务建设，也处理过性能和长期维护问题，现在希望把这些真实工程经验用于 AI 应用前端和 AI 辅助研发场景，承担更完整的技术责任。
